---
phase: 11-skill-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/jarvis-daemon/src/skill-creator.ts
  - packages/jarvis-daemon/src/skill-creator.test.ts
  - packages/jarvis-daemon/src/growth.ts
  - packages/jarvis/skills/_template/SKILL.md
  - packages/jarvis/skills/_template/tool-scaffold.ts
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03, SKILL-04, SKILL-05, SKILL-06]

must_haves:
  truths:
    - "Growth engine detects capability gaps from repeated failures and manual requests"
    - "Growth engine can write new SKILL.md files following the existing plugin skill pattern"
    - "Growth engine can write new TypeScript MCP tools with Zod schemas and tests"
    - "New skills are verified (tests pass, build succeeds) before commit"
    - "New skills are committed to a staging branch, not main"
    - "A GitHub PR is created for each new skill with description and test results"
  artifacts:
    - path: "packages/jarvis-daemon/src/skill-creator.ts"
      provides: "SkillCreator helper — gap detection, templates, branch/PR workflow"
      exports: ["SkillCreator", "GapDetectionResult"]
    - path: "packages/jarvis/skills/_template/SKILL.md"
      provides: "Template for new SKILL.md files"
      contains: "## Trigger"
    - path: "packages/jarvis/skills/_template/tool-scaffold.ts"
      provides: "Template for new MCP tool files with Zod schema"
      contains: "z.object"
  key_links:
    - from: "packages/jarvis-daemon/src/growth.ts"
      to: "packages/jarvis-daemon/src/skill-creator.ts"
      via: "Growth prompt includes skill creation instructions and gap detection data"
      pattern: "skillCreator\\|gap.*detect"
    - from: "packages/jarvis-daemon/src/skill-creator.ts"
      to: "packages/jarvis-daemon/src/state/ledger.ts"
      via: "SkillCreator queries ledger for failure patterns"
      pattern: "ledger.*getRecent"
---

<objective>
Enable Jarvis to detect capability gaps and autonomously create new tools, verified and submitted via GitHub PR.

Purpose: VOYAGER/LATM-pattern skill creation lets Jarvis grow his toolset overnight without human intervention during creation, while requiring human review before merge.
Output: SkillCreator helper class, skill templates, growth prompt instructions for skill creation workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-skill-creation/11-CONTEXT.md

@packages/jarvis-daemon/src/growth.ts
@packages/jarvis-daemon/src/state/ledger.ts
@packages/jarvis/skills/triage/SKILL.md

<interfaces>
From packages/jarvis-daemon/src/state/ledger.ts:
```typescript
export class TaskLedger {
  get database(): Database.Database
  record(entry: LedgerEntry): number
  getRecent(taskName: string, limit?: number): LedgerEntry[]
  getRecentAll(limit?: number): LedgerEntry[]
}
```

From packages/jarvis-daemon/src/state/history.ts:
```typescript
export class ChatHistory {
  constructor(db: Database.Database)
  store(taskName: string, role: string, content: string): void
  getRecent(taskName: string, limit?: number): Array<{role: string, content: string, created_at: string}>
}
```

From packages/jarvis-daemon/src/growth.ts:
```typescript
export interface GrowthConfig {
  dispatcher: Dispatcher;
  ledger: TaskLedger;
  corrections: CorrectionStore;
  taskNames: string[];
  repoRoot: string;
  // ...
}

export function buildReflectionPrompt(...): string
```

Existing skill structure (packages/jarvis/skills/triage/SKILL.md):
- ## Trigger (when to activate)
- ## Procedure (step by step)
- ## Tools (MCP tools used)
- ## Rules (decision rules)
- ## Output (expected format)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SkillCreator helper class with gap detection and templates</name>
  <files>
    packages/jarvis-daemon/src/skill-creator.ts,
    packages/jarvis-daemon/src/skill-creator.test.ts,
    packages/jarvis/skills/_template/SKILL.md,
    packages/jarvis/skills/_template/tool-scaffold.ts
  </files>
  <behavior>
    - detectGaps(ledger, minOccurrences=3) queries task_runs for failures with error containing "no tool", "not available", "cannot", "unsupported" — groups by error pattern, returns GapDetectionResult[] with { pattern, occurrences, examples }
    - getSkillTemplate() returns the SKILL.md template string
    - getToolScaffold(toolName) returns a TypeScript MCP tool template with Zod schema placeholder
    - formatGapsForPrompt(gaps) returns a formatted string for inclusion in growth prompt
    - SkillCreator is a pure helper class (no git/gh operations — those happen in the growth claude -p session)
  </behavior>
  <action>
    **SkillCreator** (packages/jarvis-daemon/src/skill-creator.ts):
    Create a class with:

    1. `detectGaps(db: Database.Database, minOccurrences: number = 3): GapDetectionResult[]`
       - Query task_runs for failures in the last 30 days
       - Search error messages for patterns: "no tool", "not available", "cannot find tool", "unsupported operation"
       - Group by normalized error pattern (strip specifics, keep the capability name)
       - Return gaps with occurrences >= minOccurrences
       - Per SKILL-01

    2. `getSkillTemplate(): string`
       - Return a SKILL.md template following the existing pattern (Trigger, Procedure, Tools, Rules, Output)
       - Per SKILL-02

    3. `getToolScaffold(toolName: string): string`
       - Return a TypeScript template with: import { z } from "zod", const schema = z.object({...}), export function, test file scaffold
       - Per SKILL-03

    4. `formatGapsForPrompt(gaps: GapDetectionResult[]): string`
       - Format detected gaps as a prompt-friendly summary for the growth engine

    **Templates** (packages/jarvis/skills/_template/):
    - SKILL.md: A template with placeholder sections matching the existing skill format (per D-02)
    - tool-scaffold.ts: A template MCP tool file with Zod schema, handler function, and test stub

    Export types: `GapDetectionResult { pattern: string; occurrences: number; examples: string[]; firstSeen: string; lastSeen: string }`
  </action>
  <verify>
    <automated>cd packages/jarvis-daemon && npx vitest run src/skill-creator.test.ts</automated>
  </verify>
  <done>
    - detectGaps() finds repeated failure patterns from the ledger
    - Templates match the existing skill pattern (triage SKILL.md as reference)
    - Tool scaffold includes Zod schema and test stub
    - formatGapsForPrompt() produces readable prompt text
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire SkillCreator into growth prompt with staging + PR instructions</name>
  <files>
    packages/jarvis-daemon/src/growth.ts
  </files>
  <action>
    **Growth engine wiring** (growth.ts):

    1. Add optional `skillCreator?: SkillCreator` to GrowthConfig (or just import and use directly since it's a pure helper).

    2. In `runGrowthLoop()`, before building the reflection prompt, call `SkillCreator.detectGaps(ledger.database)` to get current capability gaps.

    3. Add a `<capability_gaps>` section to the reflection prompt (in buildReflectionPrompt or as a new parameter) with the formatted gap data.

    4. Add a SKILL CREATION PROCEDURE section to the IMPROVE_SKILL_PROCEDURE constant (or append to buildReflectionPrompt). This section instructs the growth claude -p session on the full workflow:

    ```
    SKILL CREATION (for 'new-tool' backlog items or detected capability gaps):

    When you identify a gap that needs a new tool:

    1. BRANCH: Create a staging branch
       git checkout -b skill/{tool-name}

    2. SKILL.MD: Create packages/jarvis/skills/{name}/SKILL.md using this template:
       [include getSkillTemplate() output]

    3. MCP TOOL: Create the TypeScript MCP tool in packages/jarvis/src/tools/{name}.ts:
       - Import { z } from "zod"
       - Define input schema with z.object()
       - Implement the handler function
       - Export the tool definition

    4. TEST: Create packages/jarvis/src/tools/{name}.test.ts:
       - Test input validation
       - Test handler with mock data
       - Test error cases

    5. VERIFY:
       npm test
       npm run build
       Only proceed if BOTH pass.

    6. COMMIT:
       git add -A && git commit -m "feat(skill): add {name} tool"

    7. PR: Create a GitHub pull request:
       git push -u origin skill/{tool-name}
       gh pr create --repo pjschulz3004/claude-plugins \
         --title "Jarvis Skill: {name}" \
         --body "## New Skill\n{description}\n\n## Gap Detected\n{gap details}\n\n## Test Results\n{paste test output}"

    8. RETURN TO MAIN:
       git checkout main

    IMPORTANT: Never merge your own PR. Paul reviews all new skills.
    ```

    Per D-03: staging branch `skill/{name}`
    Per D-04: PR via `gh pr create`
    Per SKILL-04: verify before commit (tests pass, build succeeds)
    Per SKILL-05: staging branch, not main
    Per SKILL-06: GitHub PR with description and test results
  </action>
  <verify>
    <automated>cd packages/jarvis-daemon && npx vitest run src/growth.test.ts</automated>
  </verify>
  <done>
    - Growth prompt includes detected capability gaps from telemetry (SKILL-01)
    - Growth prompt includes full skill creation procedure with SKILL.md template (SKILL-02)
    - Growth prompt includes MCP tool scaffold with Zod schema pattern (SKILL-03)
    - Procedure includes verify step (npm test + npm run build) (SKILL-04)
    - Procedure uses staging branch pattern skill/{name} (SKILL-05)
    - Procedure includes gh pr create with description (SKILL-06)
    - All existing growth tests still pass
  </done>
</task>

</tasks>

<verification>
1. `cd packages/jarvis-daemon && npx vitest run` — all tests pass
2. `npm run build` from repo root — TypeScript compiles clean
3. Grep growth.ts for `capability_gaps` or `skill creation` — gap data included in prompt
4. Grep growth.ts for `skill/` — staging branch instruction present
5. Grep growth.ts for `gh pr create` — PR creation instruction present
6. Template files exist in packages/jarvis/skills/_template/
</verification>

<success_criteria>
- Gap detection queries ledger for repeated failures (SKILL-01)
- SKILL.md template matches existing skill pattern (SKILL-02)
- Tool scaffold includes Zod schema and test stub (SKILL-03)
- Growth prompt requires test + build before commit (SKILL-04)
- Growth prompt uses staging branch workflow (SKILL-05)
- Growth prompt creates GitHub PR (SKILL-06)
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/11-skill-creation/11-01-SUMMARY.md`
</output>
