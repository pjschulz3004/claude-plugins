---
phase: 03-commands
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/gsd/forge-temper.md
  - ~/.claude/commands/gsd/forge-autonomous.md
autonomous: true
requirements:
  - CMD-04
  - CMD-05

must_haves:
  truths:
    - "Running /gsd:forge-temper invokes the hardening workflow on any project without requiring forge.local.md"
    - "Running /gsd:forge-autonomous launches the Forge-native autonomous discipline loop"
    - "Running /gsd:forge-autonomous --from N starts the loop from phase N"
  artifacts:
    - path: "~/.claude/commands/gsd/forge-temper.md"
      provides: "Standalone TEMPER hardening command"
      contains: "name: gsd:forge-temper"
    - path: "~/.claude/commands/gsd/forge-autonomous.md"
      provides: "Forge-native autonomous loop command"
      contains: "name: gsd:forge-autonomous"
  key_links:
    - from: "forge-temper.md"
      to: "forge-temper workflow"
      via: "@$HOME/.claude/get-shit-done/workflows/forge-temper.md reference"
      pattern: "forge-temper\\.md"
    - from: "forge-autonomous.md"
      to: "forge-autonomous workflow"
      via: "@$HOME/.claude/get-shit-done/workflows/forge-autonomous.md reference"
      pattern: "forge-autonomous\\.md"
---

<objective>
Create two standalone GSD command files: `forge-temper.md` (standalone hardening pass, no pipeline state required) and `forge-autonomous.md` (Forge-native autonomous loop with discipline wrapper).

Purpose: These commands give users direct access to the TEMPER hardening pass and the autonomous execution loop without needing a full pipeline context.

Output:
- `~/.claude/commands/gsd/forge-temper.md`
- `~/.claude/commands/gsd/forge-autonomous.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-commands/03-CONTEXT.md

<interfaces>
<!-- GSD command file format reference -->

Standard frontmatter:
```yaml
---
name: gsd:<command-name>
description: <one-line description>
argument-hint: "[args]"
allowed-tools:
  - Read
  - Write
  - Bash
  - Task
---
```

Standard body sections:
- `<objective>` — what the command does
- `<execution_context>` — @file references loaded by GSD
- `<context>` — arguments and runtime context
- `<process>` — step-by-step instructions
- `<success_criteria>` — completion checklist

<!-- Phase 2 workflow files (already exist in ~/.claude/get-shit-done/workflows/): -->
- forge-temper.md — security review, performance audit, git diff review, simplification (no forge.local.md required)
- forge-autonomous.md — Forge-native autonomous loop, calls discipline wrapper per phase, supports --from N flag

<!-- GSD autonomous.md pattern for --from N flag handling: -->
Optional flag: `--from N` — start from phase N instead of the first incomplete phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write forge-temper.md GSD command</name>
  <files>~/.claude/commands/gsd/forge-temper.md</files>
  <read_first>
    - ~/.claude/commands/gsd/autonomous.md (format reference — note how it delegates entirely to workflow)
    - ~/.claude/get-shit-done/workflows/forge-temper.md (first 40 lines — understand what this workflow does so the command description is accurate)
  </read_first>
  <action>
Create `~/.claude/commands/gsd/forge-temper.md` as a GSD command file.

**Frontmatter:**
```yaml
---
name: gsd:forge-temper
description: Run Forge hardening pass — security review, performance audit, diff review, simplification
argument-hint: ""
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - Task
---
```

**`<objective>` section:**
Explain this runs the full TEMPER hardening pass on the current project. No forge.local.md required — works on any project at any stage. Covers four areas: security review, performance audit, git diff review, and code simplification.

**`<execution_context>` section:**
```
@$HOME/.claude/get-shit-done/workflows/forge-temper.md
```

**`<context>` section:**
```
No arguments required. Runs against current working directory.

Note: Does not require an active Forge pipeline. Can be run as a standalone hardening pass on any project.
```

**`<process>` section:**
Single instruction: Execute the forge-temper workflow from `@$HOME/.claude/get-shit-done/workflows/forge-temper.md` end-to-end. Preserve all workflow gates (security review, performance audit, git diff review, simplification pass).

**`<success_criteria>` section:**
```
- [ ] Security review completed
- [ ] Performance audit completed
- [ ] Git diff review completed
- [ ] Simplification pass completed
```
  </action>
  <verify>
    <automated>grep -l "name: gsd:forge-temper" ~/.claude/commands/gsd/forge-temper.md && grep -l "forge-temper.md" ~/.claude/commands/gsd/forge-temper.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at ~/.claude/commands/gsd/forge-temper.md
    - Frontmatter contains `name: gsd:forge-temper`
    - execution_context references forge-temper.md workflow
    - No forge.local.md dependency mentioned as required
    - success_criteria lists the four hardening areas
  </acceptance_criteria>
  <done>forge-temper.md exists with correct frontmatter and delegates to the forge-temper workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Write forge-autonomous.md GSD command</name>
  <files>~/.claude/commands/gsd/forge-autonomous.md</files>
  <read_first>
    - ~/.claude/commands/gsd/autonomous.md (direct structural reference — forge-autonomous mirrors this pattern with Forge discipline added)
    - ~/.claude/get-shit-done/workflows/forge-autonomous.md (first 40 lines — understand loop structure and --from N flag handling)
  </read_first>
  <action>
Create `~/.claude/commands/gsd/forge-autonomous.md` as a GSD command file.

**Frontmatter:**
```yaml
---
name: gsd:forge-autonomous
description: Run all remaining phases autonomously with Forge discipline — discuss→plan→execute with decomposition layer enforcement per phase
argument-hint: "[--from N]"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - AskUserQuestion
  - Task
---
```

**`<objective>` section:**
Explain this executes all remaining milestone phases autonomously, calling the Forge discipline wrapper for each phase. Unlike `/gsd:autonomous`, this version enforces decomposition layer ordering (types → logic → edge → UI → integration), runs 1-shot architecture tests between phases, and spawns specialist agents per layer. Pauses only for user decisions (grey area acceptance, blockers, validation requests). Supports `--from N` to resume from a specific phase.

**`<execution_context>` section:**
```
@$HOME/.claude/get-shit-done/workflows/forge-autonomous.md
@$HOME/.claude/get-shit-done/workflows/forge-discipline.md
```

**`<context>` section:**
```
Optional flag: `--from N` — start from phase N instead of the first incomplete phase.

Project context, phase list, and state are resolved inside the workflow using init commands. No upfront context loading needed.

forge_discipline flag: Read from .planning/config.json. The discipline wrapper self-deactivates when forge_discipline: false.
```

**`<process>` section:**
Single instruction: Execute the forge-autonomous workflow from `@$HOME/.claude/get-shit-done/workflows/forge-autonomous.md` end-to-end. Preserve all workflow gates (phase discovery, per-phase discipline execution, stall detection, blocker handling, progress display).

**`<success_criteria>` section:**
```
- [ ] All remaining phases discovered from ROADMAP.md
- [ ] Discipline wrapper called for each phase
- [ ] Decomposition layer ordering enforced per phase
- [ ] Stall detection triggers after 2 consecutive failures of same phase
- [ ] Milestone audit → complete → cleanup runs after all phases
```
  </action>
  <verify>
    <automated>grep -l "name: gsd:forge-autonomous" ~/.claude/commands/gsd/forge-autonomous.md && grep -l "forge-autonomous.md" ~/.claude/commands/gsd/forge-autonomous.md && grep -l "forge-discipline.md" ~/.claude/commands/gsd/forge-autonomous.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at ~/.claude/commands/gsd/forge-autonomous.md
    - Frontmatter contains `name: gsd:forge-autonomous`
    - frontmatter argument-hint includes "--from N"
    - execution_context references both forge-autonomous.md and forge-discipline.md
    - Task tool in allowed-tools (agent spawning)
    - success_criteria mentions discipline wrapper and decomposition layer ordering
  </acceptance_criteria>
  <done>forge-autonomous.md exists with correct frontmatter, --from N flag documented, and delegates to forge-autonomous workflow with forge-discipline.md as supporting context.</done>
</task>

</tasks>

<verification>
```bash
# Both files exist
ls -la ~/.claude/commands/gsd/forge-temper.md ~/.claude/commands/gsd/forge-autonomous.md

# Correct name fields
grep "name: gsd:forge-temper" ~/.claude/commands/gsd/forge-temper.md
grep "name: gsd:forge-autonomous" ~/.claude/commands/gsd/forge-autonomous.md

# Workflow references present
grep "forge-temper\.md" ~/.claude/commands/gsd/forge-temper.md
grep "forge-autonomous\.md" ~/.claude/commands/gsd/forge-autonomous.md
grep "forge-discipline\.md" ~/.claude/commands/gsd/forge-autonomous.md

# --from N flag documented
grep "\-\-from" ~/.claude/commands/gsd/forge-autonomous.md
```
</verification>

<success_criteria>
- `~/.claude/commands/gsd/forge-temper.md` exists with `name: gsd:forge-temper`, no forge.local.md dependency
- `~/.claude/commands/gsd/forge-autonomous.md` exists with `name: gsd:forge-autonomous`, `--from N` flag, Task in allowed-tools
- Both files reference their respective Phase 2 workflow files in `<execution_context>`
- forge-autonomous.md also references forge-discipline.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-commands/03-02-SUMMARY.md`
</output>
