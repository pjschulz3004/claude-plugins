---
phase: 02-workflows
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/get-shit-done/workflows/forge-temper.md
  - ~/.claude/get-shit-done/workflows/forge-deliver.md
autonomous: true
requirements:
  - WF-07
  - WF-08

must_haves:
  truths:
    - "forge-temper.md runs security review, performance audit, git diff review, and code simplification as a standalone pass"
    - "forge-temper.md does NOT require forge.local.md to be in a specific phase (standalone, not pipeline-dependent)"
    - "forge-deliver.md creates a PR using gh pr create with Forge-specific documentation in the PR body"
    - "forge-deliver.md reads forge.local.md to populate PR body with decomposition layers, hardening findings, and agent XP log"
    - "forge-deliver.md invokes gsd:ship for git operations rather than managing commits independently"
  artifacts:
    - path: "~/.claude/get-shit-done/workflows/forge-temper.md"
      provides: "Hardening pass — security, performance, git diff, simplification"
      contains: "security"
    - path: "~/.claude/get-shit-done/workflows/forge-deliver.md"
      provides: "PR creation with Forge documentation"
      contains: "gh pr create"
  key_links:
    - from: "forge-temper.md"
      to: "forge-reviewer agent"
      via: "Task() spawning with security/performance/simplification focus"
      pattern: "forge-reviewer"
    - from: "forge-deliver.md"
      to: "forge.local.md"
      via: "reading decomposition layers and agent XP for PR body"
      pattern: "forge.local.md"
    - from: "forge-deliver.md"
      to: "gsd:ship"
      via: "Skill() invocation for git operations"
      pattern: "gsd:ship"
---

<objective>
Write forge-temper.md and forge-deliver.md — the hardening and shipping workflows. Both are adapted from Forge skills into GSD-native workflow format.

Purpose: TEMPER hardens code (security + performance + diff review + simplification) before delivery. DELIVER creates a PR with Forge-specific documentation drawn from the pipeline history in forge.local.md.
Output: Two files at `~/.claude/get-shit-done/workflows/forge-temper.md` and `~/.claude/get-shit-done/workflows/forge-deliver.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workflows/02-CONTEXT.md

<interfaces>
<!-- Key schemas and patterns the executor needs. -->

From forge.local.md (relevant fields for deliver):
```yaml
phase: temper          # or deliver
gates:
  ignite_complete: false
  shape_complete: false
  temper_complete: false
  deliver_complete: false
agent_xp:
  forge_backend: 0
  forge_frontend: 0
  forge_reviewer: 0
  forge_tester: 0
# Plus ignite-specific fields (if IGNITE was run):
# ignite.speak_summary, ignite.approach_chosen
# decomposition.layers_complete: [1, 2, 3, 4, 5]
# temper.security_reviewed, temper.performance_reviewed, temper.diff_reviewed, temper.simplified
```

CONTEXT.md decisions:
- forge-temper.md: standalone (not dependent on forge.local.md phase tracking — skip phase check)
- forge-deliver.md: integrate with gsd:ship for PR creation; add Forge-specific documentation

forge-reviewer agent capabilities:
- Security review focus: injection, auth, secrets, CORS, rate limiting
- Performance review focus: N+1 queries, re-renders, memory leaks, caching
- Simplification focus: dead code, over-abstraction, unused imports

gsd:ship invocation:
```
Skill(skill="gsd:ship")
```
Note: gsd:ship handles git commit, push, and PR creation. forge-deliver.md should prepare the PR body content, then call gsd:ship or gh pr create directly.

Visual verification (TEMPER) — per REQUIREMENTS.md out-of-scope:
"Visual verification in TEMPER (agent-browser dependency) — optional, not required"
Include visual verification as a conditional step with a skip if agent-browser not available.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write forge-temper.md workflow</name>
  <files>~/.claude/get-shit-done/workflows/forge-temper.md</files>
  <read_first>
    - /home/paul/dev/claude/plugins/forge/skills/forge-temper/SKILL.md — the source to adapt; read every step
    - /home/paul/.claude/get-shit-done/workflows/autonomous.md — GSD workflow format (XML structure, Skill/Task patterns)
    - /home/paul/.claude/agents/forge-reviewer.md — understand what this agent does (spawned for security/performance/simplification)
    - /home/paul/.claude/agents/forge-tester.md — spawned for final test suite run
    - /home/paul/dev/claude/plugins/demiurge/.planning/phases/02-workflows/02-CONTEXT.md — decisions: standalone mode
    - /home/paul/dev/claude/plugins/demiurge/forge.local.md — state fields this workflow writes
  </read_first>
  <action>
    Create `~/.claude/get-shit-done/workflows/forge-temper.md` by adapting the Forge TEMPER skill into GSD workflow format.

    **Key adaptation from source skill:**
    - Source has "Entry" that reads forge.local.md and verifies SHAPE is complete
    - GSD version: **standalone** — skip the forge.local.md phase check entirely. The CONTEXT.md decision is: "Make standalone (not dependent on forge.local.md phase tracking)." The workflow works regardless of what forge.local.md shows.
    - Still WRITE to forge.local.md at the end of each step (so state is tracked if used in pipeline)
    - Visual verification step (Step 5 in source): keep but make it conditional on agent-browser availability and skip gracefully if not present (per REQUIREMENTS.md — this is out of scope)

    **Structure to produce:**

    ```markdown
    <purpose>

    Run a hardening pass on the current codebase. Works standalone — does not require a specific forge.local.md state. Runs: security review (forge-reviewer), performance audit (forge-reviewer), git diff review (mandatory, Point & Call), code simplification (forge-reviewer), and final test suite (forge-tester).

    **Entry**: Invoked by `/gsd:forge-temper`, from forge pipeline after SHAPE phases complete, or standalone at any point.

    </purpose>

    <required_reading>

    Read before starting:
    - `forge.local.md` — read if present to understand project context; do NOT block on phase state
    - Run `git log --oneline -10` to understand recent changes

    </required_reading>

    <process>

    <step name="security_review" priority="first">
    ## Step 1: Security Review
    [Spawn forge-reviewer with security focus]
    [Full checklist from source: input validation, auth/authz, no hardcoded secrets, SQL/XSS/injection, CORS, dependency audit, file upload, rate limiting]
    [Severity ratings: critical / high / medium / low]
    [Block on critical and high — must fix before proceeding]
    [forge.local.md write: temper.security_reviewed: true]
    </step>

    <step name="performance_audit">
    ## Step 2: Performance Audit
    [Spawn forge-reviewer with performance focus]
    [Checklist: N+1 queries, re-renders, bundle size, memory leaks, caching, async blocking]
    [Fix high-impact, log others as known tradeoffs]
    [forge.local.md write: temper.performance_reviewed: true]
    </step>

    <step name="git_diff_review">
    ## Step 3: Git Diff Review (Mandatory)
    [Run: git diff main...HEAD or git diff $(git merge-base HEAD main)...HEAD]
    [Focus areas from source: fallback logic, default values, error handling, type coercion, race conditions, off-by-one]
    [Present each finding with exact diff hunk and explanation]
    [Point & Call gate: user confirms "I have reviewed the critical logic in the diff and it is correct."]
    [forge.local.md write: temper.diff_reviewed: true]
    </step>

    <step name="simplification">
    ## Step 4: Code Simplification
    [Spawn forge-reviewer with simplification focus]
    [Look for: dead code, over-abstraction, simplifiable functions, code-restating comments, unused imports]
    [Apply conservatively — clearer, not just shorter]
    [forge.local.md write: temper.simplified: true]
    </step>

    <step name="visual_verification">
    ## Step 5: Visual Verification (Web Projects Only — Optional)
    [Check if agent-browser is available: which agent-browser 2>/dev/null]
    [Check if project has web frontend: look for index.html, package.json with dev script, src/ with .tsx/.jsx files]
    [If agent-browser not available OR no web frontend: display "Skipping visual verification — not applicable" and proceed]
    [If available: use agent-browser open, wait, screenshot, snapshot sequence from source]
    [forge.local.md write: temper.visual_verified: true (only if actually run)]
    </step>

    <step name="final_test_suite">
    ## Step 6: Final Test Suite
    [Spawn forge-tester to run complete test suite]
    [Run unit + integration + E2E (if exists)]
    [Report coverage metrics]
    [Flag flaky or skipped tests]
    [All tests must pass before proceeding — if failures exist, display and block]
    </step>

    <step name="phase_transition">
    ## Phase Transition
    [Update forge.local.md: phase: deliver, gates.temper_complete: true]
    [Announce: "TEMPER complete. Code is hardened."]
    [Display summary: which steps ran, findings addressed, test results]
    </step>

    </process>

    <success_criteria>
    [Checklist for WF-07]
    </success_criteria>
    ```

    **Critical behavioral rules to encode:**
    - Git diff review is MANDATORY — never skip, even if no findings expected
    - Point & Call at git diff step is user-confirmed (not automatic)
    - Security critical/high findings BLOCK proceeding — must be fixed
    - Visual verification is skipped gracefully (not an error) if agent-browser missing
    - forge.local.md writes happen even in standalone mode (so pipeline tracking still works if run in sequence)

    Write the full workflow with actual bash commands, actual Task() invocations, and complete checklist items embedded in the agent prompts. No placeholders.
  </action>
  <verify>
    test -f ~/.claude/get-shit-done/workflows/forge-temper.md && echo "file exists"
    grep -q "forge-reviewer" ~/.claude/get-shit-done/workflows/forge-temper.md && echo "reviewer agent present"
    grep -q "git diff" ~/.claude/get-shit-done/workflows/forge-temper.md && echo "git diff review present"
    grep -q "Point & Call" ~/.claude/get-shit-done/workflows/forge-temper.md && echo "Point & Call gate present"
    grep -q "forge-tester" ~/.claude/get-shit-done/workflows/forge-temper.md && echo "tester agent present"
    wc -l < ~/.claude/get-shit-done/workflows/forge-temper.md
    # Must be >= 100 lines
  </verify>
  <acceptance_criteria>
    - File exists: `test -f ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0
    - Has security review step: `grep -qi "security" ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0
    - Has performance audit step: `grep -qi "performance" ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0
    - Has git diff review step (mandatory): `grep -q "git diff" ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0
    - Has simplification step: `grep -qi "simplif" ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0
    - Has final test suite step: `grep -q "forge-tester" ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0
    - Has Point & Call gate: `grep -q "Point & Call" ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0
    - Spawns forge-reviewer: `grep -q "forge-reviewer" ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0
    - Does NOT block on forge.local.md phase state (standalone): `grep -qv "verify SHAPE is complete\|phase.*must be" ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0 (absence of blocking phase check)
    - Writes to forge.local.md: `grep -q "temper\." ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0
    - Has <purpose> section: `grep -q "<purpose>" ~/.claude/get-shit-done/workflows/forge-temper.md` exits 0
    - File is substantial: `wc -l < ~/.claude/get-shit-done/workflows/forge-temper.md` returns 100 or more
  </acceptance_criteria>
  <done>forge-temper.md exists in GSD workflows directory. It is standalone (no forge.local.md phase gate). It runs: security review (forge-reviewer), performance audit (forge-reviewer), git diff review with Point & Call gate, code simplification (forge-reviewer), optional visual verification (gracefully skipped if agent-browser absent), and final test suite (forge-tester). Writes temper.* flags to forge.local.md at each step.</done>
</task>

<task type="auto">
  <name>Task 2: Write forge-deliver.md workflow</name>
  <files>~/.claude/get-shit-done/workflows/forge-deliver.md</files>
  <read_first>
    - /home/paul/dev/claude/plugins/forge/skills/forge-deliver/SKILL.md — the source to adapt
    - /home/paul/.claude/get-shit-done/workflows/autonomous.md — GSD workflow format
    - /home/paul/.claude/get-shit-done/workflows/ship.md — how gsd:ship works (understand before integrating)
    - /home/paul/dev/claude/plugins/demiurge/forge.local.md — fields to read for PR body (speak_summary, approach_chosen, layers_complete, agent_xp, temper findings)
    - /home/paul/dev/claude/plugins/demiurge/.planning/phases/02-workflows/02-CONTEXT.md — decisions: gsd:ship integration, Forge-specific PR docs
  </read_first>
  <action>
    Create `~/.claude/get-shit-done/workflows/forge-deliver.md` by adapting the Forge DELIVER skill into GSD workflow format.

    **Key adaptations from source skill:**
    1. **PR creation**: Source uses `gh pr create` directly. GSD version invokes `Skill(skill="gsd:ship")` for git operations, then uses `gh pr create` directly with the Forge-enriched PR body (since gsd:ship may not know about Forge-specific content).
    2. **Forge-specific PR documentation**: The PR body must include: decomposition layers implemented, hardening findings addressed, agent XP log. Read these from forge.local.md.
    3. **Entry check**: Keep TEMPER verification from source skill (read forge.local.md gates.temper_complete), but make it a warning not a hard block if forge.local.md is missing (graceful degradation).

    **Structure to produce:**

    ```markdown
    <purpose>

    Create a PR with Forge-specific documentation and run the deployment checklist. Reads forge.local.md to include decomposition layers, security/performance findings, and agent XP log in the PR body.

    **Entry**: Invoked by `/gsd:forge` when phase is deliver, or standalone after TEMPER completes.

    </purpose>

    <required_reading>

    Read before starting:
    - `forge.local.md` — PR body content (speak_summary, approach_chosen, layers_complete, agent_xp, temper findings)
    - `git log --oneline -10` — recent commits for PR description

    </required_reading>

    <process>

    <step name="entry_check" priority="first">
    ## Entry Check
    [Read forge.local.md — check gates.temper_complete]
    [If false and forge.local.md exists: display "Warning: TEMPER not marked complete. Consider running forge-temper first. Continue anyway? (yes/no)"]
    [If forge.local.md missing: display "forge.local.md not found — will create PR with available git history only"]
    </step>

    <step name="build_pr_content">
    ## Step 1: Build PR Content
    [Read forge.local.md fields:]
    - ignite.speak_summary → PR title and summary section
    - ignite.approach_chosen → Architecture decisions section
    - decomposition.layers_complete → Layers implemented list
    - temper findings (if tracked) → Security and performance addressed section
    - agent_xp → Agent XP log section
    [Also read: git log --oneline for commit list]

    [Construct PR body with these sections:]
    ```
    ## Summary
    {from speak_summary or commit history}

    ## Architecture Decisions
    {from approach_chosen}

    ## Decomposition Layers Implemented
    {from layers_complete — format as checklist}

    ## Security & Performance (TEMPER)
    {from temper findings or "TEMPER pass completed — no critical findings"}

    ## Agent XP Log
    | Agent | Invocations | Role |
    |-------|-------------|------|
    {from agent_xp}

    ## Test Coverage
    {from test run results or "See CI"}
    ```
    </step>

    <step name="create_pr">
    ## Step 2: Create PR

    Use gh pr create with the prepared body:
    ```bash
    gh pr create --title "{title from speak_summary}" --body "$(cat <<'PREOF'
    {full PR body from step 1}
    PREOF
    )"
    ```

    Save the PR URL:
    ```bash
    PR_URL=$(gh pr view --json url -q .url)
    ```

    Update forge.local.md: deliver.pr_url = PR_URL
    </step>

    <step name="deployment_checklist">
    ## Step 3: Deployment Checklist
    [Present appropriate checklist from source: Web App / Python Package / General]
    [Ask user to confirm each item or mark N/A]
    [Use AskUserQuestion for batch confirmation]
    </step>

    <step name="agent_xp_review">
    ## Step 4: Agent XP Review
    [Read agent_xp from forge.local.md]
    [Present summary: what worked well, what caused friction, phases per agent]
    [AskUserQuestion: "Any other lessons to log?"]
    [Append user additions to forge.local.md notes]
    </step>

    <step name="completion">
    ## Step 5: Completion
    [Update forge.local.md: phase: complete, gates.deliver_complete: true, deliver.deployed: true]
    [Display FORGE COMPLETE announcement from source skill — with phase checkmarks]
    [Show PR URL]
    </step>

    </process>

    <success_criteria>
    [Checklist for WF-08]
    </success_criteria>
    ```

    **Critical behavioral rules to encode:**
    - forge.local.md is read for content but NOT a hard gate (graceful degradation if missing)
    - PR body MUST include Forge-specific sections (decomposition layers, agent XP) — this is what differentiates it from a plain PR
    - If any forge.local.md field is missing/empty, use a sensible default or "Not tracked" — never crash
    - agent_xp log format: table showing agent name, invocation count, role description
    - Deployment checklist: present appropriate type based on detecting project type (check for package.json/pyproject.toml)

    Write the full workflow with actual bash commands for reading forge.local.md fields (using python3/grep), actual gh pr create command with heredoc body, and complete deployment checklists for each project type. No placeholders.
  </action>
  <verify>
    test -f ~/.claude/get-shit-done/workflows/forge-deliver.md && echo "file exists"
    grep -q "gh pr create" ~/.claude/get-shit-done/workflows/forge-deliver.md && echo "PR creation present"
    grep -q "forge.local.md" ~/.claude/get-shit-done/workflows/forge-deliver.md && echo "state reads present"
    grep -q "agent_xp" ~/.claude/get-shit-done/workflows/forge-deliver.md && echo "XP log present"
    grep -q "decomposition\|layers_complete" ~/.claude/get-shit-done/workflows/forge-deliver.md && echo "layers documentation present"
    wc -l < ~/.claude/get-shit-done/workflows/forge-deliver.md
    # Must be >= 100 lines
  </verify>
  <acceptance_criteria>
    - File exists: `test -f ~/.claude/get-shit-done/workflows/forge-deliver.md` exits 0
    - Creates PR with gh: `grep -q "gh pr create" ~/.claude/get-shit-done/workflows/forge-deliver.md` exits 0
    - Reads forge.local.md: `grep -c "forge.local.md" ~/.claude/get-shit-done/workflows/forge-deliver.md` returns 3 or more
    - Includes decomposition layers in PR body: `grep -qi "decomposition\|layers_complete\|layers implemented" ~/.claude/get-shit-done/workflows/forge-deliver.md` exits 0
    - Includes agent XP log in PR body: `grep -q "agent_xp\|Agent XP" ~/.claude/get-shit-done/workflows/forge-deliver.md` exits 0
    - Has deployment checklist: `grep -qi "deployment\|checklist" ~/.claude/get-shit-done/workflows/forge-deliver.md` exits 0
    - Has FORGE COMPLETE announcement: `grep -qi "FORGE COMPLETE\|forge complete" ~/.claude/get-shit-done/workflows/forge-deliver.md` exits 0
    - Updates forge.local.md at completion: `grep -q "deliver_complete\|phase.*complete" ~/.claude/get-shit-done/workflows/forge-deliver.md` exits 0
    - Has <purpose> section: `grep -q "<purpose>" ~/.claude/get-shit-done/workflows/forge-deliver.md` exits 0
    - File is substantial: `wc -l < ~/.claude/get-shit-done/workflows/forge-deliver.md` returns 100 or more
  </acceptance_criteria>
  <done>forge-deliver.md exists in GSD workflows directory. It reads forge.local.md to build a PR body with Forge-specific documentation (decomposition layers, security/performance findings, agent XP log). Creates PR with gh pr create. Presents deployment checklist. Concludes with FORGE COMPLETE announcement. Gracefully handles missing forge.local.md.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Both files exist: `ls -la ~/.claude/get-shit-done/workflows/forge-temper.md ~/.claude/get-shit-done/workflows/forge-deliver.md`
2. WF-07 satisfied: `grep -E "security|performance|git diff|simplif|forge-tester" ~/.claude/get-shit-done/workflows/forge-temper.md`
3. WF-08 satisfied: `grep -E "gh pr create|agent_xp|decomposition|layers" ~/.claude/get-shit-done/workflows/forge-deliver.md`
4. Temper is standalone: no phase-blocking check on forge.local.md state
5. Deliver gracefully handles missing forge.local.md
</verification>

<success_criteria>
- forge-temper.md is standalone — runs at any point without checking forge.local.md phase state
- forge-temper.md covers all 4 hardening areas: security, performance, git diff (with Point & Call), simplification
- forge-temper.md includes final test suite via forge-tester agent
- forge-deliver.md creates PR body populated from forge.local.md fields (decomposition, agent XP, temper findings)
- forge-deliver.md gracefully degrades if forge.local.md is absent
- Both files follow GSD XML workflow format with <purpose>, <process>, <success_criteria>
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflows/02-03-SUMMARY.md` using the summary template.
</output>
