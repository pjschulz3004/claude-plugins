---
phase: 02-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/get-shit-done/workflows/forge-ignite.md
autonomous: true
requirements:
  - WF-01
  - WF-02

must_haves:
  truths:
    - "forge-ignite.md guides a session through SPEAK -> EXPLORE -> SHAPE_DOMAIN -> FIRST_1000_LINES in sequence"
    - "Each step ends with a Point & Call gate requiring the user to state a verbal commitment"
    - "Each step writes state to forge.local.md before proceeding to the next"
    - "At FIRST_1000_LINES completion, the workflow invokes gsd:new-project instead of managing its own state"
  artifacts:
    - path: "~/.claude/get-shit-done/workflows/forge-ignite.md"
      provides: "IGNITE phase behavioral script for GSD agents"
      contains: "SPEAK"
  key_links:
    - from: "forge-ignite.md"
      to: "forge.local.md"
      via: "YAML frontmatter writes at each step"
      pattern: "forge.local.md"
    - from: "forge-ignite.md"
      to: "gsd:new-project"
      via: "Skill() invocation at completion"
      pattern: "gsd:new-project"
---

<objective>
Write forge-ignite.md — the workflow file that guides a Claude session through Forge's IGNITE phase: SPEAK, EXPLORE, SHAPE_DOMAIN, and FIRST_1000_LINES.

Purpose: Encode Forge's foundation discipline as a GSD-native workflow. The IGNITE phase is the most critical — it forces System 2 thinking before any code is written, establishing types-first decomposition and Point & Call gates.
Output: `~/.claude/get-shit-done/workflows/forge-ignite.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workflows/02-CONTEXT.md

<interfaces>
<!-- Key schemas and formats the executor needs. -->

From forge.local.md (Phase 1 output):
```yaml
schema_version: "1.0"
phase: ignite          # ignite | shape | temper | deliver
step: speak            # IGNITE steps: speak | explore | shape_domain | first_1000_lines
gates:
  ignite_complete: false
  shape_complete: false
  temper_complete: false
  deliver_complete: false
health_checks:
  types_defined: false
  logic_pure: false
  boundaries_thin: false
  ui_isolated: false
  integration_tested: false
decomposition:
  current_layer: 1
  layers_complete: []
agent_xp:
  forge_backend: 0
  forge_frontend: 0
  forge_designer: 0
  forge_tester: 0
  forge_reviewer: 0
  forge_researcher: 0
```

Forge agents available at ~/.claude/agents/:
- forge-researcher.md — explores existing codebases
- forge-backend.md — Layer 1-3 implementation (types, logic, edges)
- forge-frontend.md — Layer 4 implementation (UI components)
- forge-designer.md — design system and visual review
- forge-reviewer.md — code review and security
- forge-tester.md — test writing and execution

GSD workflow format (from autonomous.md):
- XML sections: <purpose>, <required_reading>, <process>, <success_criteria>
- Steps use <step name="..." priority="..."> XML tags
- Skill() invocations for other GSD commands: Skill(skill="gsd:new-project")
- Bash commands in fenced code blocks
- AskUserQuestion for interactive gates

Decomposition template at:
~/.claude/get-shit-done/templates/forge-decomposition.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write forge-ignite.md workflow</name>
  <files>~/.claude/get-shit-done/workflows/forge-ignite.md</files>
  <read_first>
    - /home/paul/dev/claude/plugins/forge/skills/forge-ignite/SKILL.md — the source to adapt; read every step carefully
    - /home/paul/.claude/get-shit-done/workflows/autonomous.md — the GSD workflow format to follow (XML structure, Skill() calls, AskUserQuestion usage)
    - /home/paul/.claude/get-shit-done/templates/forge-decomposition.md — the decomposition layer template to reference in SHAPE_DOMAIN step
    - /home/paul/dev/claude/plugins/demiurge/forge.local.md — the state schema this workflow reads and writes
  </read_first>
  <action>
    Create `~/.claude/get-shit-done/workflows/forge-ignite.md` by adapting the Forge IGNITE skill into GSD workflow format. The file must follow GSD's XML structure with <purpose>, <process>, and <success_criteria> sections.

    **Key adaptations from the source skill:**

    1. **Replace skill invocation with GSD format**: The source uses skills internally. The GSD version uses AskUserQuestion for interactive gates and Skill() for GSD command invocations.

    2. **State tracking**: Every step that writes to forge.local.md should show the exact bash command to update the YAML frontmatter field. Use `sed` or `python3 -c` to update specific keys without clobbering the full file.

    3. **Phase transition at completion**: Instead of invoking the `forge-shape` skill, invoke GSD's project creation:
       ```
       Skill(skill="gsd:new-project")
       ```
       This is the GSD-compatible output required by WF-02.

    4. **Agent spawning syntax**: When spawning forge-researcher, use Task() with the agent:
       ```
       Task(agent="forge-researcher", prompt="Explore the existing codebase structure...")
       ```

    **Structure to produce:**

    ```markdown
    <purpose>

    Guide a Claude session through Forge's IGNITE phase: SPEAK → EXPLORE → SHAPE_DOMAIN → FIRST_1000_LINES. The most critical phase — the first few thousand lines determine everything that follows. Forces System 2 thinking through Point & Call discipline before any code is written.

    **Entry**: Invoked by `/gsd:forge new` command or when forge.local.md shows phase: ignite.

    </purpose>

    <required_reading>

    Read these files before starting:
    - `forge.local.md` — current pipeline state (create with defaults if missing)
    - `~/.claude/get-shit-done/templates/forge-decomposition.md` — layer decomposition template

    </required_reading>

    <process>

    <step name="speak" priority="first">
    ## Step 1: SPEAK
    [Full SPEAK step content — AskUserQuestion for each of the 3 questions, one at a time]
    [Point & Call gate requiring user to state the system summary]
    [forge.local.md update: step: explore, ignite.speak_summary written]
    </step>

    <step name="explore">
    ## Step 2: EXPLORE
    [Research step — spawn forge-researcher if existing codebase]
    [context7 or WebSearch for dependencies]
    [Present 2-3 approaches with recommendation leading]
    [Point & Call gate requiring user to state chosen approach]
    [forge.local.md update: step: shape_domain, ignite.approach_chosen written]
    </step>

    <step name="shape_domain">
    ## Step 3: SHAPE THE DOMAIN
    [5-layer decomposition — reference the decomposition template]
    [Each layer: define → present to user → AskUserQuestion gate → update forge.local.md flag]
    [Layer 1 gate: data_model_approved]
    [Layer 2 gate: pure_logic_approved]
    [Layer 3 gate: edge_logic_approved]
    [Layer 4 gate: ui_approved (if applicable)]
    [Layer 5 gate: integration_approved]
    </step>

    <step name="first_1000_lines">
    ## Step 4: FIRST 1000 LINES
    [Project structure generation]
    [Ask user for project type: web-ts | web-py | python | custom]
    [Write types from Layer 1, signatures from Layer 2]
    [Spawn forge-tester for TDD]
    [Spawn forge-reviewer for foundation code review]
    [1-Shot Prompt Test: ask user to describe a feature, attempt to add it]
    [Point & Call gate: user states "The foundation is solid"]
    [forge.local.md update: ignite.foundation_passed: true, phase: shape, step: gsd_handoff, gates.ignite_complete: true]
    </step>

    <step name="gsd_handoff">
    ## Phase Transition
    [Announce IGNITE complete]
    [Invoke gsd:new-project to hand off to GSD for project lifecycle management]

    Skill(skill="gsd:new-project")
    </step>

    </process>

    <success_criteria>
    [Checklist of observable behaviors]
    </success_criteria>
    ```

    **Critical behavioral rules to encode in the workflow:**

    - "Do not suggest solutions" in SPEAK — ask questions only, wait for user to articulate
    - "Do not jump to planning" in EXPLORE — if user asks for a plan, explain why not yet
    - "Problems in Layer 1 cascade to everything below" — reinforce review weight on types
    - AskUserQuestion gates must be blocking (user must respond before proceeding)
    - Point & Call phrases are specific: "The system takes [X] as input and produces [Y] for [Z]", "I chose approach [A] because [reason]", "I can add a feature in 1 shot. The foundation is solid."

    **forge.local.md write pattern** (use this in each step):
    ```bash
    python3 -c "
    import re, sys
    with open('forge.local.md', 'r') as f:
        content = f.read()
    # Update step
    content = re.sub(r'^step: .*$', 'step: explore', content, flags=re.MULTILINE)
    with open('forge.local.md', 'w') as f:
        f.write(content)
    print('Updated forge.local.md: step -> explore')
    "
    ```

    Write the full workflow file with complete content for each step — not placeholder text. Every AskUserQuestion must have specific question text and options. Every forge.local.md write must show the actual field being updated.
  </action>
  <verify>
    test -f ~/.claude/get-shit-done/workflows/forge-ignite.md && echo "file exists" && grep -c "SPEAK\|EXPLORE\|SHAPE_DOMAIN\|FIRST_1000_LINES\|shape_domain\|first_1000_lines" ~/.claude/get-shit-done/workflows/forge-ignite.md
    # Must show file exists and return count >= 4 (all four steps present)
    grep -q "gsd:new-project" ~/.claude/get-shit-done/workflows/forge-ignite.md && echo "GSD handoff present"
    grep -q "forge.local.md" ~/.claude/get-shit-done/workflows/forge-ignite.md && echo "state writes present"
    grep -q "Point & Call" ~/.claude/get-shit-done/workflows/forge-ignite.md && echo "Point & Call gates present"
  </verify>
  <acceptance_criteria>
    - File exists: `test -f ~/.claude/get-shit-done/workflows/forge-ignite.md` exits 0
    - Has all four steps: `grep -qE "step name=\"(speak|explore|shape_domain|first_1000_lines)\"" ~/.claude/get-shit-done/workflows/forge-ignite.md` (check each individually)
    - Has GSD handoff: `grep -q "gsd:new-project" ~/.claude/get-shit-done/workflows/forge-ignite.md` exits 0
    - Has state writes: `grep -q "forge.local.md" ~/.claude/get-shit-done/workflows/forge-ignite.md` exits 0
    - Has Point & Call gates: `grep -c "Point & Call" ~/.claude/get-shit-done/workflows/forge-ignite.md` returns 3 or more
    - Has all 5 decomposition layers: `grep -c "Layer [12345]" ~/.claude/get-shit-done/workflows/forge-ignite.md` returns 5 or more
    - Has <purpose> section: `grep -q "<purpose>" ~/.claude/get-shit-done/workflows/forge-ignite.md` exits 0
    - Has <success_criteria> section: `grep -q "<success_criteria>" ~/.claude/get-shit-done/workflows/forge-ignite.md` exits 0
    - Has agent spawning (forge-researcher): `grep -q "forge-researcher" ~/.claude/get-shit-done/workflows/forge-ignite.md` exits 0
    - File is substantial (>= 150 lines): `wc -l < ~/.claude/get-shit-done/workflows/forge-ignite.md` returns 150 or more
  </acceptance_criteria>
  <done>forge-ignite.md exists in GSD workflows directory with all four IGNITE steps, Point & Call gates, forge.local.md state writes, and gsd:new-project handoff at completion. A Claude executor reading this file knows exactly what to say to the user at each step, what questions to ask, what state to write, and when to hand off to GSD.</done>
</task>

</tasks>

<verification>
After task completion:

1. File exists and is readable: `cat ~/.claude/get-shit-done/workflows/forge-ignite.md | head -5`
2. All steps present: `grep -E "step name=" ~/.claude/get-shit-done/workflows/forge-ignite.md`
3. GSD compatibility: `grep "gsd:new-project" ~/.claude/get-shit-done/workflows/forge-ignite.md`
4. WF-01 satisfied: SPEAK -> EXPLORE -> SHAPE_DOMAIN -> FIRST_1000_LINES steps all present
5. WF-02 satisfied: gsd:new-project invoked at completion (not forge-shape skill)
</verification>

<success_criteria>
- forge-ignite.md is installed at ~/.claude/get-shit-done/workflows/forge-ignite.md
- All four IGNITE steps are present with full behavioral content (not placeholders)
- Point & Call gates appear at end of SPEAK, EXPLORE, and FIRST_1000_LINES steps
- forge.local.md is read at entry and written after each step
- Phase transition invokes Skill(skill="gsd:new-project") — not a Forge-internal skill
- A Claude agent reading this file can execute the full IGNITE phase without ambiguity
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflows/02-01-SUMMARY.md` using the summary template.
</output>
