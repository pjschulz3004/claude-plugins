---
phase: 05-improve-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - ~/.claude/commands/gsd/improve-phase.md
autonomous: true
requirements:
  - IMP-01
  - IMP-05

must_haves:
  truths:
    - "improve-phase.md exists at ~/.claude/commands/gsd/"
    - "Command accepts --duration and --category flags and documents their defaults"
    - "Command execution_context references forge-improve-phase.md workflow"
    - "~/.claude/commands/improve.md is unmodified (structural check passes)"
  artifacts:
    - path: "~/.claude/commands/gsd/improve-phase.md"
      provides: "GSD slash command entry point for improve-phase"
      contains: "gsd:improve-phase"
  key_links:
    - from: "~/.claude/commands/gsd/improve-phase.md"
      to: "~/.claude/get-shit-done/workflows/forge-improve-phase.md"
      via: "execution_context reference"
---

# Plan 05-02: Write gsd:improve-phase Command + Verify No Regression

<objective>
Write the improve-phase.md GSD command file and verify that the standalone /improve command is unmodified.

Purpose: Give users a /gsd:improve-phase entry point that accepts --duration and --category flags and delegates to the forge-improve-phase.md workflow. Confirm the standalone command is untouched (IMP-05).

Output: ~/.claude/commands/gsd/improve-phase.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-improve-integration/05-CONTEXT.md
@.planning/phases/05-improve-integration/05-01-SUMMARY.md

<interfaces>
Command format reference — read ~/.claude/commands/gsd/forge-autonomous.md to see the exact structure.
The command file uses YAML frontmatter (name, description, argument-hint, allowed-tools) followed by
objective, execution_context, context, process, and success_criteria XML blocks.

From 05-CONTEXT.md (locked decisions):
  - Command install path: ~/.claude/commands/gsd/improve-phase.md
  - Flags: --duration (e.g. 30m, 1h, 2h, default: 30m) and --category (e.g. security,quality, default: security,quality)
  - Supported category values: security, quality, simplification, testing, performance, design
  - Unsupported in GSD mode: visual/ui, feature_creep
  - Routes to: ~/.claude/get-shit-done/workflows/forge-improve-phase.md
  - Must NOT modify ~/.claude/commands/improve.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write improve-phase.md command file</name>
  <files>~/.claude/commands/gsd/improve-phase.md</files>
  <read_first>
  - ~/.claude/commands/gsd/forge-autonomous.md (reference command format — copy the structure exactly)
  - ~/.claude/get-shit-done/workflows/forge-improve-phase.md (verify it was written correctly in Plan 01)
  - .planning/phases/05-improve-integration/05-CONTEXT.md (design decisions)
  </read_first>
  <action>
Write ~/.claude/commands/gsd/improve-phase.md. Mirror the structure of forge-autonomous.md exactly.

YAML frontmatter fields:
  name: gsd:improve-phase
  description: Run an autonomous Improve rotation cycle as a GSD phase — scoped categories, bounded duration, produces VERIFICATION.md
  argument-hint: "[--duration 30m|1h|2h] [--category security,quality,simplification,testing,performance,design]"
  allowed-tools list: Read, Write, Edit, Bash, Glob, Grep, Task, AskUserQuestion, mcp__context7__resolve-library-id, mcp__context7__query-docs

objective block content:
  - Runs Improve's category rotation cycle scoped to specified categories
  - Default: 30m duration, security + quality categories
  - Produces VERIFICATION.md with status: passed|gaps_found in project root
  - Does NOT create .improve-log.md or .improve-research.md (those are standalone /improve only)
  - Results: VERIFICATION.md in project root, commits per cycle

execution_context block:
  @$HOME/.claude/get-shit-done/workflows/forge-improve-phase.md

context block content:
  - --duration defaults to 30m. Supported: 30m, 1h, 2h (and combinations like 1h30m)
  - --category defaults to security,quality. Supported: security, quality, simplification, testing, performance, design
  - NOT supported in GSD mode: visual/ui, feature_creep (use standalone /improve for those)
  - The workflow reads .planning/ for GSD phase context automatically — no need to pass it
  - Requires clean git working tree before starting

process block content:
  "Execute the forge-improve-phase workflow from @$HOME/.claude/get-shit-done/workflows/forge-improve-phase.md end-to-end. Pass $ARGUMENTS through to the workflow. Preserve all workflow gates: autopilot lifecycle, hard time gate (no Phase 2 while REMAINING > 300s), cycle rotation order (security first), stall detection."

success_criteria checklist:
  - [ ] .autopilot marker created at start, removed at end
  - [ ] Time gate enforced (REMAINING < 300s → Phase 2 immediately)
  - [ ] Requested categories run in priority order (security always first if included)
  - [ ] VERIFICATION.md written to project root with status: passed|gaps_found
  - [ ] All commits use format: improve(<category>): <description>
  </action>
  <verify>
    <automated>ls ~/.claude/commands/gsd/improve-phase.md && grep -c "gsd:improve-phase\|forge-improve-phase\|--duration\|--category" ~/.claude/commands/gsd/improve-phase.md</automated>
  </verify>
  <done>
  - File exists at ~/.claude/commands/gsd/improve-phase.md
  - YAML frontmatter has name: gsd:improve-phase
  - argument-hint documents --duration and --category flags with defaults
  - execution_context block references forge-improve-phase.md workflow
  - success_criteria checklist covers all IMP-01 through IMP-04 behaviors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify standalone /improve has no regression</name>
  <files></files>
  <read_first>
  - ~/.claude/commands/improve.md (read the full file to verify structure is intact)
  </read_first>
  <action>
Verify ~/.claude/commands/improve.md was not modified during Phase 5.

Run these structural checks — all must pass:

  grep -c "improve-log.md" ~/.claude/commands/improve.md
  grep -c "improve-research.md" ~/.claude/commands/improve.md
  grep -c "Feature Creep" ~/.claude/commands/improve.md
  grep -c "Visual/UI" ~/.claude/commands/improve.md
  grep -c "creep-build" ~/.claude/commands/improve.md

All five checks must return a count of 1 or more.

Also confirm the two command files are separate and distinct:
  diff ~/.claude/commands/improve.md ~/.claude/commands/gsd/improve-phase.md | head -3

Expect: non-empty diff output (they are different files with different content).

If any check fails, STOP and report the discrepancy. Do not attempt to fix improve.md.
  </action>
  <verify>
    <automated>grep -c "improve-log.md" ~/.claude/commands/improve.md && grep -c "Feature Creep" ~/.claude/commands/improve.md</automated>
  </verify>
  <done>
  - All 5 grep checks return count >= 1
  - diff confirms the two command files are distinct
  - No modification to ~/.claude/commands/improve.md was made during Phase 5
  </done>
</task>

</tasks>

<verification>
grep -q "gsd:improve-phase" ~/.claude/commands/gsd/improve-phase.md
grep -q "forge-improve-phase.md" ~/.claude/commands/gsd/improve-phase.md
grep -q "improve-log.md" ~/.claude/commands/improve.md
grep -q "Feature Creep" ~/.claude/commands/improve.md
</verification>

<success_criteria>
- improve-phase.md command file written following GSD command format
- Command clearly documents --duration and --category flags with defaults
- Workflow reference correct (forge-improve-phase.md in execution_context)
- Standalone /improve verified unmodified — IMP-05 confirmed structurally
</success_criteria>

<output>
After completion, create .planning/phases/05-improve-integration/05-02-SUMMARY.md
</output>
